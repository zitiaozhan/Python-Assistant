"""Built-in capability plugins.

Concrete plugins (clipboard sync, notifications, file transfer, ...) will
live here. This package is intentionally empty for now — it only reserves
the namespace and documents where new plugins should go.
"""
